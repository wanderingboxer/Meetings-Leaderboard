kind = "web"
previewPath = "/"
title = "GoComet Dashboard"
version = "1.0.0"
id = "artifacts/gocomet-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19104

[services.development]
run = "pnpm --filter @workspace/gocomet-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gocomet-dashboard", "run", "build" ]
publicDir = "artifacts/gocomet-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19104"
BASE_PATH = "/"
